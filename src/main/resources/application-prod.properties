# === PROD PROFILE ===
# In production, NEVER hardcode. Everything comes from environment variables.
server.port=${SERVER_PORT:8443}

# External APIs via environment variables (managed by GCP Secret Manager)
app.api.viacep.url=${VIACEP_URL}
app.api.second.url=${NATIONALIZE_URL}

# Minimal logging in production (WARN and ERROR only)
logging.level.root=WARN
logging.level.com.spring_base.fundamentals=INFO
